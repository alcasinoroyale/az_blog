<nav>
  <div class="home">
    <h1> <%= link_to "AZ Blog", root_path %> </h1>
  </div>

  <div class="navbar">
    <% if logged_in? %>
      <%= link_to "Profile", user_path(current_user) %>
      <%= link_to "Logout", logout_path, method: :delete %>
    <% else %>
      <%= link_to "Join", signup_path %>
      <%= link_to "Login", login_path %>
    <% end %>
  </div>

</nav>
